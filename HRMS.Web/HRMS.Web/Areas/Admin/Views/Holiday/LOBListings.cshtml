@using HRMS.Models.Common;

<link href="~/assets/pages/gridlist.css" rel="stylesheet" />

@{
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "LOB";

    var success = TempData["Success"];
    var Err = TempData["Error"];
}

<div class="main-header anim" style="--delay: 0s">
    LOB List

    <a href="~/Admin/Holiday/AddLOB">
        <button type="button" class="btn btn-addBtn">
            Add LOB
        </button>
    </a>
</div>

<div class="table-responsive py-3">

    <table id="tblLOBListing"
           class="table table-striped body-text"
           style="width:100%">
        <thead>
            <tr>
                <th style="display:none;">ID</th>
                <th>LOB Name</th>
                <th>Status</th>
                <th>Action</th>
            </tr>
        </thead>
    </table>

</div>

<script>

    $(document).ready(function () {

        $('#tblLOBListing').DataTable({

            "ajax": {
                "url": "LOBListings/",
                "type": "POST",
                "datatype": "json",
                "dataSrc": "data"
            },

            "ordering": true,
            "order": [[0, "desc"]],
            "paging": true,
            "searching": true,
            "info": true,

            "columns": [

                {
                    "data": "lobid",
                    "visible": false
                },

                {
                    "data": "name",
                    "autoWidth": true
                },

                {
                    "data": "isActive",
                    "autoWidth": true,
                    "render": function (data) {
                        return data ? "Active" : "Inactive";
                    }
                },

                {
                    mRender: function (data, type, row) {

                        return '<div class="table-data-feature">' +
                            '<a class="item" href="AddLOB?id=' + row.encodedId + '">' +
                            '<img src="/assets/img/edit.png" width="20" height="20" />' +
                            '</a>' +
                            '</div>';
                    }
                }
            ],

            "responsive": true
        });

    });

</script>